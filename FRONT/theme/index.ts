import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    darkT: {
      100: "#2121213b",
    },
    cyan: {
      100: "#2C7A7B",
      200: "#256D85"
    },
  },
});

export default theme;
